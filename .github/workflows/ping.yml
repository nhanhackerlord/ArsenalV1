name: Run Bot After 31 Minutes

on:
  schedule:
    - cron: "0,31 * * * *"  # Chạy mỗi 31 phút

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Grand Perm
        run: |
          chmod 777 tls
      - name: Run the bot
        run: |
          python3 bot.py
